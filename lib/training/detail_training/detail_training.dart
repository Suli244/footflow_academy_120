import 'package:flutter/material.dart';

class DetailTraining extends StatelessWidget {
  const DetailTraining({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DetailTraining'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}
